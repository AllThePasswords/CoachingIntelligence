---
phase: 03-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Dashboard/Dashboard.jsx
  - src/pages/Dashboard/index.js
  - src/pages/ManagerDetail/ManagerDetail.jsx
  - src/pages/ManagerDetail/index.js
  - src/pages/index.js
autonomous: true

must_haves:
  truths:
    - "Dashboard page displays 4 manager cards in a 2-column grid"
    - "Dashboard page has TimeframeToggle in header area"
    - "ManagerDetail page shows manager name and back link"
    - "Pages can be imported from @/pages barrel export"
  artifacts:
    - path: "src/pages/Dashboard/Dashboard.jsx"
      provides: "Dashboard page with card grid"
      min_lines: 25
    - path: "src/pages/ManagerDetail/ManagerDetail.jsx"
      provides: "Manager detail placeholder page"
      min_lines: 20
    - path: "src/pages/index.js"
      provides: "Page barrel exports"
      exports: ["Dashboard", "ManagerDetail"]
  key_links:
    - from: "src/pages/Dashboard/Dashboard.jsx"
      to: "@/components/cards"
      via: "ManagerCard import"
      pattern: "import.*ManagerCard.*from.*@/components/cards"
    - from: "src/pages/Dashboard/Dashboard.jsx"
      to: "@/data"
      via: "managers data import"
      pattern: "import.*managers.*from.*@/data"
    - from: "src/pages/ManagerDetail/ManagerDetail.jsx"
      to: "react-router"
      via: "useParams and Link imports"
      pattern: "import.*(useParams|Link).*from.*react-router"
---

<objective>
Create the Dashboard and ManagerDetail page components with proper structure and navigation hooks.

Purpose: Build the two pages that make up the Phase 3 dashboard experience. Dashboard shows all managers in a grid; ManagerDetail is a placeholder that will be fully implemented in Phase 4.

Output: Dashboard page with card grid + timeframe toggle, ManagerDetail placeholder with back link, page barrel exports
</objective>

<execution_context>
@/Users/ericgreene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ericgreene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard/03-RESEARCH.md

# Existing components to use
@src/components/cards/ManagerCard/ManagerCard.jsx
@src/components/input/TimeframeToggle/TimeframeToggle.jsx
@src/data/managers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dashboard page component</name>
  <files>src/pages/Dashboard/Dashboard.jsx, src/pages/Dashboard/index.js</files>
  <action>
Create `src/pages/Dashboard/` directory and Dashboard component:

**Dashboard.jsx:**
1. Import useNavigate from 'react-router'
2. Import ManagerCard from '@/components/cards'
3. Import TimeframeToggle from '@/components/input'
4. Import managers from '@/data'

Structure:
- Container: `max-w-7xl mx-auto px-4 py-8`
- Header row: `flex justify-between items-center mb-8`
  - Title: `text-2xl font-semibold tracking-tight` with text "Team Overview"
  - TimeframeToggle component
- Card grid: `grid grid-cols-1 md:grid-cols-2 gap-6`
  - Map over managers array
  - Render ManagerCard for each with managerId={manager.id}
  - Pass onClick handler that calls navigate(`/manager/${managerId}`)

**index.js:**
- Export Dashboard as named export from Dashboard.jsx

Follow RESEARCH.md Pattern 3 (Responsive Card Grid) and Pattern 5 (useNavigate).
  </action>
  <verify>
- File exists: `ls src/pages/Dashboard/Dashboard.jsx`
- Has useNavigate: `grep -n "useNavigate" src/pages/Dashboard/Dashboard.jsx`
- Has managers import: `grep -n "managers" src/pages/Dashboard/Dashboard.jsx`
- Has grid layout: `grep -n "grid-cols" src/pages/Dashboard/Dashboard.jsx`
  </verify>
  <done>
- Dashboard renders 4 ManagerCards in responsive 2-column grid
- TimeframeToggle visible in header area
- onClick navigates to /manager/:managerId
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ManagerDetail placeholder page</name>
  <files>src/pages/ManagerDetail/ManagerDetail.jsx, src/pages/ManagerDetail/index.js</files>
  <action>
Create `src/pages/ManagerDetail/` directory and ManagerDetail component:

**ManagerDetail.jsx:**
1. Import useParams, Link from 'react-router'
2. Import getManagerById from '@/data'

Structure:
- Container: `max-w-7xl mx-auto px-4 py-8`
- Back link: Link to "/" with className `text-sm text-gray-500 hover:text-foreground mb-4 inline-block`
  - Text: "← Back to Dashboard"
- Extract managerId from useParams()
- Get manager data: getManagerById(managerId)
- If no manager found: show "Manager not found: {managerId}" message with back link
- If found:
  - Manager name: `text-3xl font-bold tracking-tight mb-2`
  - Region: `text-gray-500 mb-4`
  - Placeholder text: "Detail view coming in Phase 4"

**index.js:**
- Export ManagerDetail as named export

Follow RESEARCH.md Pattern 6 (useParams).
  </action>
  <verify>
- File exists: `ls src/pages/ManagerDetail/ManagerDetail.jsx`
- Has useParams: `grep -n "useParams" src/pages/ManagerDetail/ManagerDetail.jsx`
- Has Link: `grep -n "Link" src/pages/ManagerDetail/ManagerDetail.jsx`
- Has back navigation: `grep -n "Back to Dashboard" src/pages/ManagerDetail/ManagerDetail.jsx`
  </verify>
  <done>
- ManagerDetail extracts managerId from URL params
- Shows manager name if found, error if not
- Back link navigates to dashboard
  </done>
</task>

<task type="auto">
  <name>Task 3: Create pages barrel export</name>
  <files>src/pages/index.js</files>
  <action>
Create `src/pages/index.js` barrel export:

```javascript
export { Dashboard } from './Dashboard';
export { ManagerDetail } from './ManagerDetail';
```

This enables clean imports: `import { Dashboard, ManagerDetail } from '@/pages'`
  </action>
  <verify>`grep -n "export" src/pages/index.js` shows both Dashboard and ManagerDetail exports</verify>
  <done>Both page components exportable from @/pages barrel</done>
</task>

</tasks>

<verification>
1. `src/pages/` directory exists with Dashboard/, ManagerDetail/, and index.js
2. Dashboard imports and renders ManagerCard grid
3. Dashboard has useNavigate for card click handling
4. ManagerDetail uses useParams to get managerId
5. ManagerDetail has Link to navigate back to dashboard
6. Pages barrel exports both components
</verification>

<success_criteria>
- Dashboard page shows 4 managers in 2-column grid
- TimeframeToggle present and functional (connected to existing Zustand store)
- ManagerDetail shows basic info and back link
- Both pages ready to be wired to router
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard/03-02-SUMMARY.md`
</output>
