---
phase: 03-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/main.jsx
  - src/App.jsx
  - src/components/cards/ManagerCard/ManagerCard.jsx
autonomous: true

must_haves:
  truths:
    - "App renders with BrowserRouter wrapping Routes"
    - "Root path (/) renders Dashboard inside AppLayout"
    - "Manager path (/manager/:managerId) renders ManagerDetail"
    - "ManagerCard has pink gradient border option"
  artifacts:
    - path: "src/App.jsx"
      provides: "Router configuration with layout routes"
      contains: "Routes"
    - path: "src/main.jsx"
      provides: "BrowserRouter wrapper"
      contains: "BrowserRouter"
    - path: "src/components/cards/ManagerCard/ManagerCard.jsx"
      provides: "Gradient border variant"
      contains: "gradient"
  key_links:
    - from: "src/main.jsx"
      to: "react-router"
      via: "BrowserRouter import"
      pattern: "import.*BrowserRouter.*from.*react-router"
    - from: "src/App.jsx"
      to: "@/layouts"
      via: "AppLayout import"
      pattern: "import.*AppLayout.*from.*@/layouts"
    - from: "src/App.jsx"
      to: "@/pages"
      via: "Dashboard and ManagerDetail imports"
      pattern: "import.*(Dashboard|ManagerDetail).*from.*@/pages"
---

<objective>
Wire up React Router configuration and add gradient accent styling to ManagerCard for DASH-07.

Purpose: Connect all the pieces - layouts, pages, and routing - to create a working navigation flow. Also implement the pink gradient border accent per design spec.

Output: Working routing between Dashboard and ManagerDetail, gradient-accented manager cards
</objective>

<execution_context>
@/Users/ericgreene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ericgreene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard/03-RESEARCH.md
@.planning/phases/03-dashboard/03-01-SUMMARY.md
@.planning/phases/03-dashboard/03-02-SUMMARY.md

# Files to modify
@src/main.jsx
@src/App.jsx
@src/components/cards/ManagerCard/ManagerCard.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure routing in main.jsx and App.jsx</name>
  <files>src/main.jsx, src/App.jsx</files>
  <action>
**main.jsx:**
1. Import BrowserRouter from 'react-router' (NOT react-router-dom)
2. Wrap App component with BrowserRouter

```jsx
import { BrowserRouter } from 'react-router';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
);
```

**App.jsx:**
1. Remove ALL existing demo content
2. Import Routes, Route from 'react-router'
3. Import AppLayout from '@/layouts'
4. Import Dashboard, ManagerDetail from '@/pages'

Replace with clean router configuration:
```jsx
import { Routes, Route } from 'react-router';
import { AppLayout } from '@/layouts';
import { Dashboard, ManagerDetail } from '@/pages';

function App() {
  return (
    <Routes>
      <Route element={<AppLayout />}>
        <Route path="/" element={<Dashboard />} />
        <Route path="/manager/:managerId" element={<ManagerDetail />} />
      </Route>
    </Routes>
  );
}

export default App;
```

This uses the Layout Route pattern from RESEARCH.md - AppLayout wraps all pages via Outlet.
  </action>
  <verify>
- main.jsx has BrowserRouter: `grep -n "BrowserRouter" src/main.jsx`
- App.jsx has Routes: `grep -n "Routes" src/App.jsx`
- App.jsx imports AppLayout: `grep -n "AppLayout" src/App.jsx`
- App.jsx imports Dashboard: `grep -n "Dashboard" src/App.jsx`
  </verify>
  <done>
- BrowserRouter wraps App in main.jsx
- App.jsx has route configuration for / and /manager/:managerId
- AppLayout wraps both routes as layout route
  </done>
</task>

<task type="auto">
  <name>Task 2: Add gradient border option to ManagerCard</name>
  <files>src/components/cards/ManagerCard/ManagerCard.jsx</files>
  <action>
Update ManagerCard to support gradient border for DASH-07:

1. Add `gradient` prop (default: false)
2. When gradient=true, wrap card content in gradient border container:
   - Outer: `p-0.5 bg-gradient-to-r from-highlight to-purple rounded-xl`
   - Inner (existing card): adjust rounded to `rounded-[calc(0.75rem-2px)]` to match

Implementation approach:
```jsx
export function ManagerCard({ managerId, onClick, variant = 'border', gradient = false }) {
  // ... existing logic ...

  const content = (
    <div className={variantClasses}>
      {/* existing card content */}
    </div>
  );

  if (gradient) {
    return (
      <div className="p-0.5 bg-gradient-to-r from-highlight to-purple rounded-xl">
        {content}
      </div>
    );
  }

  return content;
}
```

Note: The gradient colors (--color-highlight: #ff0080, --color-purple: #f81ce5) are already defined in the Tailwind theme from Phase 1. The `from-highlight` and `to-purple` classes should work.

If Tailwind doesn't recognize these, fall back to direct values:
`bg-gradient-to-r from-[#ff0080] to-[#f81ce5]`
  </action>
  <verify>
- Has gradient prop: `grep -n "gradient" src/components/cards/ManagerCard/ManagerCard.jsx`
- Has gradient classes: `grep -n "from-highlight\|from-\[#ff0080\]" src/components/cards/ManagerCard/ManagerCard.jsx`
  </verify>
  <done>
- ManagerCard accepts gradient prop
- When gradient=true, card has pink gradient border
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Dashboard to use gradient cards</name>
  <files>src/pages/Dashboard/Dashboard.jsx</files>
  <action>
Update Dashboard to render ManagerCards with gradient=true for the accent effect.

In the grid mapping:
```jsx
{managers.map(manager => (
  <ManagerCard
    key={manager.id}
    managerId={manager.id}
    onClick={handleCardClick}
    gradient={true}
  />
))}
```

This satisfies DASH-07: "Cards display with pink gradient accent per design spec"
  </action>
  <verify>`grep -n "gradient" src/pages/Dashboard/Dashboard.jsx` shows gradient={true}</verify>
  <done>All manager cards display with pink gradient border accent</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. Root URL (/) shows Dashboard with 4 gradient-bordered cards
3. Clicking a card navigates to /manager/{id}
4. Manager detail page shows manager name and back link
5. Back link returns to dashboard
6. "Coaching Intelligence" header visible on both pages
7. "Ask anything" input pinned to bottom on both pages
8. TimeframeToggle changes update card metrics
</verification>

<success_criteria>
- Navigation works between Dashboard and ManagerDetail
- All 4 manager cards have pink gradient border accent
- Header and pinned input visible on all pages
- URL updates when navigating (deep linking works)
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard/03-03-SUMMARY.md`
</output>
