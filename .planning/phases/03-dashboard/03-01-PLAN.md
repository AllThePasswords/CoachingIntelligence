---
phase: 03-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/layouts/AppLayout.jsx
  - src/layouts/index.js
autonomous: true

must_haves:
  truths:
    - "App has a header showing 'Coaching Intelligence' title"
    - "AskInput is pinned to bottom of viewport"
    - "Main content area has padding to avoid fixed footer overlap"
  artifacts:
    - path: "src/layouts/AppLayout.jsx"
      provides: "Shared layout with header, Outlet, pinned footer"
      min_lines: 30
    - path: "src/layouts/index.js"
      provides: "Layout barrel export"
      exports: ["AppLayout"]
  key_links:
    - from: "src/layouts/AppLayout.jsx"
      to: "react-router"
      via: "Outlet component import"
      pattern: "import.*Outlet.*from.*react-router"
    - from: "src/layouts/AppLayout.jsx"
      to: "@/components/input"
      via: "AskInput import"
      pattern: "import.*AskInput.*from.*@/components/input"
---

<objective>
Install React Router v7 and create the shared AppLayout component with header and pinned AskInput footer.

Purpose: Establish the layout foundation that all pages share - the header (NAV-01) and pinned "Ask anything" input (DASH-06). This layout wraps page content via React Router's Outlet pattern.

Output: react-router installed, AppLayout component with header + Outlet + pinned AskInput, barrel export
</objective>

<execution_context>
@/Users/ericgreene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ericgreene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard/03-RESEARCH.md

# Existing components to use
@src/components/input/AskInput/AskInput.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React Router v7</name>
  <files>package.json</files>
  <action>
Install react-router package (v7+). Use the single package name - NOT react-router-dom (that's the old v6 pattern).

```bash
npm install react-router
```

Verify the package is added to dependencies.
  </action>
  <verify>`npm ls react-router` shows version 7.x installed</verify>
  <done>react-router ^7.x in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create AppLayout with header and pinned footer</name>
  <files>src/layouts/AppLayout.jsx, src/layouts/index.js</files>
  <action>
Create `src/layouts/` directory and AppLayout component:

**AppLayout.jsx:**
1. Import Outlet from 'react-router' (NOT react-router-dom)
2. Import AskInput from '@/components/input'
3. Structure:
   - Outer wrapper: `min-h-screen bg-background`
   - Header: `sticky top-0 z-40 bg-background-100 border-b border-border`
     - Max width container: `max-w-7xl mx-auto px-4 h-16 flex items-center`
     - Title: `text-xl font-semibold tracking-tight` with text "Coaching Intelligence"
   - Main: `pb-24` (padding for fixed footer)
     - Render `<Outlet />` here
   - Fixed footer: `fixed bottom-0 inset-x-0 z-50 bg-background-100 border-t border-border`
     - Max width container: `max-w-7xl mx-auto px-4 py-4`
     - Render AskInput with placeholder "Ask anything about your team..."
     - onSubmit handler: console.log the question and show alert (chat feature coming later)

**index.js:**
- Export AppLayout as named export

Follow the exact pattern from RESEARCH.md Pattern 2 and Pattern 4.
  </action>
  <verify>
- File exists: `ls src/layouts/AppLayout.jsx`
- Has Outlet import: `grep -n "Outlet" src/layouts/AppLayout.jsx`
- Has AskInput import: `grep -n "AskInput" src/layouts/AppLayout.jsx`
- Has "Coaching Intelligence" text: `grep -n "Coaching Intelligence" src/layouts/AppLayout.jsx`
  </verify>
  <done>
- AppLayout renders header with "Coaching Intelligence" title
- Main area renders Outlet for page content
- AskInput pinned to bottom with fixed positioning
- Main has pb-24 to avoid content being hidden behind footer
  </done>
</task>

</tasks>

<verification>
1. `npm ls react-router` shows v7.x
2. `src/layouts/` directory exists with AppLayout.jsx and index.js
3. AppLayout imports Outlet from 'react-router' (correct v7 import path)
4. AppLayout imports and renders AskInput component
5. Header has "Coaching Intelligence" text
</verification>

<success_criteria>
- react-router ^7.x installed
- AppLayout component created with proper structure
- Layout can be used as a route wrapper in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard/03-01-SUMMARY.md`
</output>
