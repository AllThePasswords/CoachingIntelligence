---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/App.jsx
autonomous: false

must_haves:
  truths:
    - "App loads in browser with Geist Sans and Geist Mono fonts applied"
    - "Pink gradient accent and neutral gray color palette render correctly"
    - "Static data can be imported and displayed"
    - "Citation lookup returns correct feedback record"
  artifacts:
    - path: "src/App.jsx"
      provides: "Foundation verification UI"
      min_lines: 20
  key_links:
    - from: "src/App.jsx"
      to: "src/data/index.js"
      via: "data imports"
      pattern: "import.*from.*data"
---

<objective>
Create comprehensive foundation verification page and validate all Phase 1 requirements.

Purpose: Confirms that all foundation pieces work together before building components. Catches integration issues early.

Output: Visual verification page demonstrating fonts, colors, and data layer working correctly.
</objective>

<execution_context>
@/Users/ericgreene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ericgreene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create foundation verification page</name>
  <files>
    src/App.jsx
  </files>
  <action>
    Update src/App.jsx with a comprehensive verification page that tests all foundation elements:

    ```jsx
    import {
      managers,
      getManagerById,
      aes,
      getAEsByManager,
      feedbackLog,
      timeframes,
      getTimeframeData,
      isValidCitationId,
      lookupCitation,
      extractCitations
    } from './data';

    function App() {
      const sarah = getManagerById('MGR001');
      const davidAEs = getAEsByManager('MGR004');
      const citation = lookupCitation('CALL-1042');
      const timeframe30 = getTimeframeData('30');

      // Test citation extraction
      const testText = 'See [-> CALL-1042] and CALL-1089 for examples.';
      const foundCitations = extractCitations(testText);

      return (
        <div className="min-h-screen bg-bg-page p-8">
          {/* Header */}
          <header className="mb-8">
            <h1 className="text-4xl font-bold text-text-primary mb-2">
              Coaching Intelligence
            </h1>
            <p className="text-text-secondary">
              Phase 1: Foundation Verification
            </p>
          </header>

          {/* Font Test */}
          <section className="bg-bg-card rounded-lg p-6 mb-6 shadow-sm">
            <h2 className="text-xl font-semibold text-text-primary mb-4">
              Typography (NAV-04)
            </h2>
            <p className="text-text-secondary mb-2">
              This text should render in Geist Sans - clean, modern, sans-serif.
            </p>
            <p className="font-mono text-text-tertiary bg-bg-page px-3 py-2 rounded">
              CALL-1042 | font-mono | Geist Mono Variable
            </p>
            <p className="text-sm text-text-muted mt-2">
              Check: Sans-serif body text, monospace citations with distinct character widths.
            </p>
          </section>

          {/* Color Test */}
          <section className="bg-bg-card rounded-lg p-6 mb-6 shadow-sm">
            <h2 className="text-xl font-semibold text-text-primary mb-4">
              Color System (NAV-05)
            </h2>

            <div className="grid grid-cols-2 gap-4 mb-4">
              {/* Text colors */}
              <div>
                <p className="text-text-primary">text-primary (#171717)</p>
                <p className="text-text-secondary">text-secondary (#525252)</p>
                <p className="text-text-tertiary">text-tertiary (#737373)</p>
                <p className="text-text-muted">text-muted (#A3A3A3)</p>
              </div>

              {/* Status colors */}
              <div>
                <p className="text-status-success">status-success (#22C55E)</p>
                <p className="text-status-warning">status-warning (#F59E0B)</p>
                <p className="text-status-error">status-error (#EF4444)</p>
                <p className="text-status-info">status-info (#3B82F6)</p>
              </div>
            </div>

            {/* Gradient */}
            <div className="bg-card-gradient p-4 rounded-lg border border-border-light">
              <p className="text-text-primary">
                Pink gradient card background (bg-card-gradient)
              </p>
              <span className="text-gradient-pink font-semibold">
                Gradient text (text-gradient-pink)
              </span>
            </div>
          </section>

          {/* Data Layer Test */}
          <section className="bg-bg-card rounded-lg p-6 mb-6 shadow-sm">
            <h2 className="text-xl font-semibold text-text-primary mb-4">
              Data Layer
            </h2>

            <div className="grid grid-cols-2 gap-6">
              {/* Managers */}
              <div>
                <h3 className="font-semibold text-text-primary mb-2">
                  Managers ({managers.length})
                </h3>
                <ul className="text-sm text-text-secondary space-y-1">
                  {managers.map(m => (
                    <li key={m.id}>
                      {m.name}: {m.coaching_score}%
                      <span className={`ml-2 ${m.trend === 'declining' ? 'text-status-error' : 'text-status-success'}`}>
                        {m.trend}
                      </span>
                    </li>
                  ))}
                </ul>
              </div>

              {/* Timeframes */}
              <div>
                <h3 className="font-semibold text-text-primary mb-2">
                  Timeframe: 30 days
                </h3>
                <p className="text-sm text-text-secondary">
                  Benchmark: {timeframe30.benchmark} calls
                </p>
                <p className="text-sm text-text-secondary">
                  Working days: {timeframe30.working_days}
                </p>
                <p className="text-sm text-text-tertiary mt-1">
                  Sarah's 30-day score: {timeframe30.managers.MGR001.coaching_score}%
                </p>
              </div>
            </div>
          </section>

          {/* Citation Test */}
          <section className="bg-bg-card rounded-lg p-6 mb-6 shadow-sm">
            <h2 className="text-xl font-semibold text-text-primary mb-4">
              Citation System
            </h2>

            <div className="mb-4">
              <h3 className="font-semibold text-text-primary mb-2">Validation</h3>
              <ul className="text-sm text-text-secondary space-y-1">
                <li>
                  <code className="font-mono bg-bg-page px-1">CALL-1042</code>:
                  {isValidCitationId('CALL-1042') ? ' valid' : ' invalid'}
                </li>
                <li>
                  <code className="font-mono bg-bg-page px-1">CALL-42</code>:
                  {isValidCitationId('CALL-42') ? ' valid' : ' invalid'}
                </li>
                <li>
                  <code className="font-mono bg-bg-page px-1">call-1042</code>:
                  {isValidCitationId('call-1042') ? ' valid' : ' invalid'}
                </li>
              </ul>
            </div>

            <div className="mb-4">
              <h3 className="font-semibold text-text-primary mb-2">Lookup: CALL-1042</h3>
              {citation && (
                <div className="text-sm text-text-secondary bg-bg-page p-3 rounded">
                  <p><strong>Date:</strong> {citation.date}</p>
                  <p><strong>AE:</strong> {citation.ae_name}</p>
                  <p><strong>Stage:</strong> {citation.pipeline_stage}</p>
                  <p className="mt-2 text-text-tertiary italic">
                    "{citation.feedback.substring(0, 100)}..."
                  </p>
                </div>
              )}
            </div>

            <div>
              <h3 className="font-semibold text-text-primary mb-2">Extraction</h3>
              <p className="text-sm text-text-secondary mb-1">
                Input: "{testText}"
              </p>
              <p className="text-sm text-text-secondary">
                Found: {foundCitations.join(', ')}
              </p>
            </div>
          </section>

          {/* AE Data Test */}
          <section className="bg-bg-card rounded-lg p-6 shadow-sm">
            <h2 className="text-xl font-semibold text-text-primary mb-4">
              David Park's Team (Critical)
            </h2>
            <table className="w-full text-sm">
              <thead>
                <tr className="text-left text-text-tertiary border-b border-border-light">
                  <th className="pb-2">AE</th>
                  <th className="pb-2">Quota %</th>
                  <th className="pb-2">Calls Coached</th>
                  <th className="pb-2">Flag</th>
                </tr>
              </thead>
              <tbody>
                {davidAEs.map(ae => (
                  <tr key={ae.id} className="border-b border-border-light">
                    <td className="py-2 text-text-primary">{ae.name}</td>
                    <td className="py-2 text-text-secondary">{ae.quota}%</td>
                    <td className="py-2 text-text-secondary">{ae.calls_coached}</td>
                    <td className={`py-2 ${ae.flag === 'critical' ? 'text-status-error font-semibold' : 'text-text-muted'}`}>
                      {ae.flag || '-'}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </section>

          {/* Footer */}
          <footer className="mt-8 text-center text-text-muted text-sm">
            Phase 1 Foundation Complete - All systems operational
          </footer>
        </div>
      );
    }

    export default App;
    ```
  </action>
  <verify>
    Run `npm run dev` and visually confirm all sections render correctly.
  </verify>
  <done>
    Verification page displays all foundation elements.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 foundation:
    1. Vite 6 + React 19 project scaffolded
    2. Tailwind CSS 4 with @theme design tokens configured
    3. Geist Sans and Geist Mono fonts installed and applied
    4. Pink gradient accent and neutral gray color palette available
    5. Static data layer with all managers, AEs, feedback, timeframes
    6. Citation utilities for validation, parsing, and lookup
    7. Verification page demonstrating all elements working together
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` if not already running
    2. Open http://localhost:5173 in browser
    3. Verify each section:

    **Typography (NAV-04):**
    - [ ] Body text uses Geist Sans (clean, modern sans-serif)
    - [ ] Monospace text (CALL-1042) uses Geist Mono (distinct character widths)

    **Color System (NAV-05):**
    - [ ] Text colors show gray gradient from dark to light
    - [ ] Status colors are distinct (green success, amber warning, red error, blue info)
    - [ ] Pink gradient card has subtle pink-to-purple gradient
    - [ ] Gradient text shows pink-to-purple gradient

    **Data Layer:**
    - [ ] Shows "Managers (4)" with Sarah Chen at 97%
    - [ ] Shows 30-day benchmark of 45 calls
    - [ ] Shows Sarah's 30-day score of 95%

    **Citation System:**
    - [ ] CALL-1042: valid
    - [ ] CALL-42: invalid
    - [ ] call-1042: invalid
    - [ ] Lookup shows Jan 22, Alex Rivera, Negotiation
    - [ ] Extraction finds CALL-1042 and CALL-1089

    **David's Team Table:**
    - [ ] Shows 4 AEs with Jessica Huang at 54% quota
    - [ ] "critical" flags show in red

    4. Run `npm run build` - should complete without errors

    5. Check browser DevTools:
    - [ ] No console errors
    - [ ] Computed font-family on body includes "Geist Variable"
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass, or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when human verifies all foundation elements work correctly.
</verification>

<success_criteria>
- [ ] Human confirms typography renders correctly (Geist Sans + Mono)
- [ ] Human confirms color system renders correctly (pink gradient, neutral grays)
- [ ] Human confirms data layer works (managers, AEs, feedback displayed)
- [ ] Human confirms citation utilities work (validation, lookup, extraction)
- [ ] `npm run build` succeeds
- [ ] No console errors in browser
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
