---
phase: 02-component-library
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Badge/Badge.jsx
  - src/components/ui/Badge/index.js
  - src/components/ui/Tooltip/Tooltip.jsx
  - src/components/ui/Tooltip/index.js
  - src/components/ui/index.js
autonomous: true

must_haves:
  truths:
    - "Badge component renders improving/declining/steady states with correct colors"
    - "Tooltip shows content on hover with arrow pointer"
    - "Components importable via @/components/ui barrel"
  artifacts:
    - path: "src/components/ui/Badge/Badge.jsx"
      provides: "Status badge with trend icon"
      exports: ["Badge"]
    - path: "src/components/ui/Tooltip/Tooltip.jsx"
      provides: "CSS-only hover tooltip"
      exports: ["Tooltip"]
    - path: "src/components/ui/index.js"
      provides: "UI components barrel export"
      exports: ["Badge", "Tooltip", "TrendArrow", "InfoIcon"]
  key_links:
    - from: "src/components/ui/Badge/Badge.jsx"
      to: "src/components/ui/TrendArrow"
      via: "import for trend icon"
      pattern: "import.*TrendArrow.*from"
---

<objective>
Create Badge and Tooltip atomic components, plus the main UI barrel export.

Purpose: Badge and Tooltip are used by multiple composite components (ManagerCard, MetricDisplay, InsightSection). The UI barrel export provides clean import paths for all atomic components.
Output: Working Badge component with three trend states, CSS-only Tooltip, and consolidated UI exports.
</objective>

<execution_context>
@/Users/ericgreene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ericgreene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-component-library/02-RESEARCH.md

# Source patterns to extract from
@src/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Badge component</name>
  <files>
    src/components/ui/Badge/Badge.jsx
    src/components/ui/Badge/index.js
  </files>
  <action>
1. Create `src/components/ui/Badge/` directory

2. Create `src/components/ui/Badge/Badge.jsx`:
   - Import TrendArrow from '../TrendArrow' (will exist from Plan 01)
   - Props: `trend` ('improving' | 'declining' | 'steady'), optional `showIcon` (default true)
   - Render badge with:
     - improving: bg-success/10 text-success, TrendArrow direction="up"
     - declining: bg-error/10 text-error, TrendArrow direction="down"
     - steady: bg-gray-100 text-foreground, TrendArrow direction="steady"
   - Badge text: capitalize trend value ("Improving", "Declining", "Steady")
   - Use exact styling from App.jsx lines 186-189, 243-246, 300-303:
     ```
     inline-flex items-center gap-1 px-2 py-1 rounded-md text-xs font-medium
     ```

3. Create `src/components/ui/Badge/index.js`:
   ```javascript
   export { Badge } from './Badge';
   ```

Match the exact Geist-style badge patterns from App.jsx. Colors use CSS variables: text-success, text-error, text-foreground.
  </action>
  <verify>
    - File exists at correct path
    - Component exports correctly
    - Styling matches App.jsx patterns
  </verify>
  <done>
    - Badge component renders three states with correct colors
    - Optional showIcon prop controls TrendArrow visibility
    - Barrel export works
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Tooltip component and UI barrel export</name>
  <files>
    src/components/ui/Tooltip/Tooltip.jsx
    src/components/ui/Tooltip/index.js
    src/components/ui/index.js
  </files>
  <action>
1. Create `src/components/ui/Tooltip/` directory

2. Create `src/components/ui/Tooltip/Tooltip.jsx`:
   - Extract Tooltip component from App.jsx lines 46-54
   - Props: `children` (trigger element), `content` (tooltip text)
   - Use exact same CSS classes for positioning, animation, and arrow
   - Key classes from App.jsx:
     ```
     relative group inline-flex items-center
     absolute bottom-full left-1/2 -translate-x-1/2 mb-2 px-3 py-2
     bg-foreground text-background-100 text-xs rounded-md
     opacity-0 invisible group-hover:opacity-100 group-hover:visible
     transition-all duration-200 whitespace-nowrap z-10 shadow-lg
     ```
   - Arrow: `absolute top-full left-1/2 -translate-x-1/2 border-4 border-transparent border-t-foreground`

3. Create `src/components/ui/Tooltip/index.js`:
   ```javascript
   export { Tooltip } from './Tooltip';
   ```

4. Create `src/components/ui/index.js` (main barrel export):
   ```javascript
   // Atomic UI components
   export { TrendArrow } from './TrendArrow';
   export { Badge } from './Badge';
   export { Tooltip } from './Tooltip';
   export { InfoIcon } from './icons';
   ```

This barrel export allows: `import { Badge, Tooltip, TrendArrow } from '@/components/ui'`
  </action>
  <verify>
    - All files created at correct paths
    - UI barrel exports all components
    - `npm run build` succeeds
  </verify>
  <done>
    - Tooltip component shows on hover with arrow
    - UI barrel exports Badge, Tooltip, TrendArrow, InfoIcon
    - Clean import path: @/components/ui
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` - should succeed
2. Verify file structure:
   ```
   src/components/ui/
   ├── Badge/
   │   ├── Badge.jsx
   │   └── index.js
   ├── Tooltip/
   │   ├── Tooltip.jsx
   │   └── index.js
   ├── TrendArrow/
   │   └── ...
   ├── icons/
   │   └── ...
   └── index.js
   ```
3. Check that barrel export includes all components
</verification>

<success_criteria>
- Badge renders three trend states with correct Geist styling
- Tooltip appears on hover with arrow pointer
- All UI components exportable via single barrel import
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-library/02-02-SUMMARY.md`
</output>
